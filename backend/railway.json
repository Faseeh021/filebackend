{
  "$schema": "https://railway.app/railway.schema.json",
  "$comment": "Railway will auto-detect Node.js. If issues persist, use Dockerfile.",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
