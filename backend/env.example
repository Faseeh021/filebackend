# Server Configuration
PORT=5000
NODE_ENV=production

# Database Configuration
# MongoDB connection string
# For local development:
DATABASE_URL=mongodb://localhost:27017/project
# For production (e.g., MongoDB Atlas):
# DATABASE_URL=mongodb+srv://username:password@cluster.mongodb.net/project

# Optional: CORS Configuration (comma-separated origins)
# Set this to your frontend URL (e.g., your Vercel URL or localhost for development)
# Example: CORS_ORIGIN=https://your-app.vercel.app,http://localhost:5173
CORS_ORIGIN=http://localhost:3000,http://localhost:5173,http://localhost:3001
