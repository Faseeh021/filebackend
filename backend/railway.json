{
  "$schema": "https://railway.app/railway.schema.json",
  "$comment": "Using Dockerfile for clean builds - explicitly disable Nixpacks",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "./Dockerfile"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
